apiVersion: 1

providers:
  - name:            liora_dashboards
    type:            file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
